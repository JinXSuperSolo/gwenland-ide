{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the GwenLand IDE main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
